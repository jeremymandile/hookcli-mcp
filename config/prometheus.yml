global:
  scrape_interval: 10s

scrape_configs:
  - job_name: "hookcli-mcp"
    static_configs:
      - targets: ["otel-collector:8888"]
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]
